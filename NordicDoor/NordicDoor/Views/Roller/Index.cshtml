@model IEnumerable<Roller>
@{ ViewData["Title"] = "Index";

}

<partial name="_Notification" />
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Roller</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Roller" asp-action="Opprett" class="btn btn-primary">
                <i class="bi bi-plus-square"></i> &nbsp; Opprett Rolle
            </a>
        </div>
    </div>
    <br />

    <table class="table table-bordered table-striped" style="width:100%">

        <thead>
            <tr>
                <th>
                    Rollenummer
                </th>
                <th>
                    Ansattnummer
                </th>
                <th>
                    Ansvar
                </th>
                <th>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width:"50%">
                        @obj.Rolle_ID
                    </td>

                    <td width:"30%">
                        @obj.Ansatt_ID
                    </td>
                    <td width:"30%">
                        @obj.Ansvar
                    </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Roller" asp-action="Rediger" asp-route-Ansatt_ID="@obj.Ansatt_ID"
                               class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Rediger</a>
                            <a asp-controller="Roller" asp-action="Slett" asp-route-Ansatt_ID="@obj.Ansatt_ID"
                               class="btn btn-danger mx-2"> <i class="bi bi-trash3-fill"></i> Slett</a>

                        </div>
                    </td>

                </tr>
            }
        </tbody>

    </table>

</div>
